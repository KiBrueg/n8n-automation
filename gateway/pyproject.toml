[tool.ruff]
line-length = 110
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]  # длина строки контролируется line-length, но не валит билд

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
